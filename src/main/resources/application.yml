server:
  port: 8080
spring:
  datasource:
    url: jdbc:hsqldb:mem:/localhost:9001/db
    driverClassName: org.hsqldb.jdbcDriver
    username: root
    password: pass
  jpa:
    show-sql: true
    open-in-view: false